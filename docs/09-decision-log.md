# Decision Log

Record decisions that change the architecture, scope or data model.

| Date | Decision | Reason | Alternatives considered | Approved by |
|---|---|---|---|---|
| | Build the permanent system as a separate Frappe app | Scale, auditability and maintainability | Permanent Custom HTML Block implementation | |
| | Keep objective mapping separate from metric mapping | They answer different governance and calculation questions | One combined mapping record | |
| | Store each answer as a reportable row | Supports filtering, drill-down and audits | One JSON response blob only | |
| | Perform official scoring on the server | Prevents tampering and ensures consistent calculations | Browser-only scoring | |
| 2026-07-24 | Normalise once at the metric layer; the index applies weights only | Prevents double-normalisation when real (already 0-100) Metric Results feed an index; keeps a single source of truth for scaling | Normalising again at each index node (found during the Step 1 integration audit to corrupt scores) | |
| 2026-07-24 | Add a metric-calculation step (answers → Metric Result) and backfill answer_numeric | The Survey→Index chain had no code turning answers into Metric Results; index only read them | Leaving Metric Results to be populated manually / externally | |
| 2026-07-26 | Finding A: a campaign requires an existing Survey Management record (option 2) | educ_sg makes Survey Tracking.survey_name mandatory. Auto-creating a stub would put planning documents nobody planned into the institutional register that this app exists to produce evidence from; relaxing the field via a Property Setter would weaken a constraint educ_sg's own code may rely on. D1 bends: Survey Management stays reference-only for AUTHORING, but is a prerequisite for COLLECTING. | Auto-create a stub; relax survey_name; keep two campaign types | Already enforced by educ_sg + survey_tracking_hooks; the work was making it legible in the UI |
| 2026-07-26 | Lineage is snapshotted onto UCC Score Breakdown at calculation time, not read live | Question Mapping is live and keeps changing. Reading it at report time meant an old result's lineage reshaped whenever someone remapped a question while its numbers stayed fixed - unacceptable for Criterion 7.1.1 evidence, which is shown externally. | Dated caveat on a live read; a separate snapshot DocType | Three Small Text fields, no new DocType. Only helps results calculated after it lands, so the report flags pre-snapshot results rather than showing them as untraceable |
| 2026-07-26 | A shared metric's contribution is repeated per objective, never split | 5 of 265 real questions carry more than one objective. Splitting invents arithmetic that appears in no calculation; attributing to a 'primary' objective silently drops the rest. | Split evenly; pick a primary | Objective sections carry no total, deliberately - no such number exists |
| 2026-07-28 | Conditional display is evaluated in BOTH places, and only the server's verdict counts | Decision V1 recorded the landmine: logic shipped client-side only would let a hidden *required* question block submission, and would let a crafted POST dodge a branch's required check by omitting it. `display_logic.py` is one pure rule; `submit_survey` recomputes visibility from the SUBMITTED answers and enforces `is_required` only on visible questions, dropping answers to hidden ones. The browser copy is presentation. | Client-side only (the landmine); a server-rendered multi-request wizard | Rules fail CLOSED - a rule pointing at a deleted question hides its question rather than stranding respondents behind something unanswerable. Both copies make the same choice, so they cannot disagree |
| 2026-07-28 | Skip To Section and End Survey dropped from the display_logic Select | Neither was ever wired to anything, and under decision V5 (sectioning is a marker question type) there is no section identifier for a skip to target. Removed rather than left as options that silently do nothing. | Designing section targets so the options could work | Reinstate with a real target model if UCC ever asks for skip logic |
| 2026-07-28 | Multi-page uses a "Page Break" question type, not a page field or page doctype | Sectioning already works this way (V5). A page is the run of questions between two markers, so paging needs no schema beyond one Select option and no change to sequence, reorder or answer storage. | A page_number field per question; a UCC Survey Page doctype | All pages submit in one POST - no partial save, which is a separate feature with its own storage question |
| 2026-07-28 | Question layout is one width field on a 12-column CSS grid; no 2D coordinates | CSS Grid derives rows from document order plus each item's span, so the existing 1D `sequence` plus one Select IS a 2D layout. `grid-auto-flow` stays `row`, never `dense` - dense back-fills gaps and would show questions out of their numbered order. | x/y coordinates per question; a row/column container doctype | Mobile collapse is a media query, no JS. Layout freezes at publish like every other question edit |
| 2026-07-28 | No theme editor: the public form's colours are CSS custom properties set once in Website Theme | `/survey` already inherits the site theme via `templates/web.html`; only the form's own controls were hardcoded. One college, one brand - a per-survey picker invites twelve differently-coloured surveys. A staff-editable free-text CSS field on the one guest-reachable page is a stored-injection surface. | Per-survey colour pickers; a raw CSS field; a theme DocType | If ever needed: a closed Select of brand presets with validated values, never a CSS blob |
| 2026-07-28 | A published version's CONTENT is frozen; its PRESENTATION is not | The invariant immutability protects is "every published score must be reproducible", and a score derives from question text, type and choices plus the answers given. `layout_width` feeds none of that - not metric_engine, not index_engine, not mapping, not lineage, not Score Breakdown - so nothing downstream of a question can observe it. Freezing it meant the only way to fix a layout that read badly on a phone was a new version, which orphans the campaign already collecting against the published one. | Leave layout frozen and require a new version (orphans the campaign); an "unlock" flag on the version (a switch that turns immutability off is not immutability) | `versioning.PRESENTATION_FIELDS` is a WHITELIST, never a blacklist: every field added to a question later is frozen by default and exempting one has to be a deliberate act. `presentation_only_change()` returns False the moment anything else differs, including choice ORDER and re-parenting, so the exemption cannot be used to smuggle a content edit through beside a width change. Deliberately ungated and visible - the Inspector shows an Apply Width button on a published version and says why - because adjusting a live survey's layout without spinning a version is the point. Frappe's `track_changes` was already on for UCC Survey Question, so each presentation edit lands in the Version log with its diff at no extra cost. |
| 2026-07-28 | Width is resized by dragging in Preview, not in the Questions list | Preview is the only place in the builder that renders the real 12-column grid, so it is the only place a width handle shows what it is doing. Behind an explicit "Edit layout" toggle so the sheet is never accidentally an editor. | Handles in the Questions list (would put a 2D control in a 1D list and risk the working index-based drag-reorder: "drop between 3 and 4" is unambiguous in a list and genuinely ambiguous in a grid, and a mousedown inside a `draggable="true"` card starts an HTML5 drag as soon as the pointer moves); a blank-canvas designer | Snaps to the four `layout_width` values - there is no pixel width field and there must not be one. Dashboard Studio was believed to have card resize handles to reuse; it has none. The mousedown -> document mousemove/mouseup shape comes from `node_canvas.js:_makeDraggable` |
| 2026-07-28 | No blank-canvas layout designer | CSS Grid already derives rows from document order plus span, so `sequence` (drag-reorder) plus `layout_width` (drag-resize) IS a 2D layout. What a free canvas adds beyond them is holes - empty cells, which in a questionnaire read as "something failed to load" - and visual order differing from document order, which is an accessibility regression: screen readers and keyboard tab order follow DOM order, so a respondent using either would traverse the form in an order contradicting both the visual layout and the numbering. Same reason `grid-auto-flow: dense` is banned. | A canvas with per-question row/column/span (~400-600 lines, a second layout path in survey.html, and a permanent second way to express layout) | Any future design surface writes STRUCTURED fields only. `www/survey.html` is the only guest-reachable page in this app: a staff-editable HTML/CSS blob rendered there is stored injection (CSS exfiltrates via attribute selectors and `url()`; a `</style>` escapes the block and becomes XSS on the page collecting real responses, against an anonymous respondent). Colours are matched against `^#[0-9a-f]{6}$`, fonts come from a closed list |
| 2026-07-28 | One renderer for the respondent form: `public/js/survey_form.js` + `public/css/ucc_survey_form.bundle.css`, shared by `www/survey.html` and the Builder's Preview | Preview was a parallel implementation - its own per-type widget switch, its own grid renderer, its own copy of the page-split rule and ~50 lines of `.ucc-sb-*` CSS shadowing `.ucc-*` - which is why it needed the duplicate pagination and still could not show real styling. The earlier "no new external JS bundle on the guest path" constraint was about third-party libraries (survey-core), not our own assets. | Keeping two renderers in sync by hand (the check_repo guard that watched the duplicated page-split rule was the cost of this, and is now deleted); a third-party form library | SUBMISSION IS NOT IN THE SHARED MODULE: the fetch, the CSRF header and the form-encoded body stay inline in `www/survey.html` and arrive as `opts.onSubmit`, so the guest submission path is unchanged and unreachable from the Desk. No `onSubmit` → no submit button. Assets load per page via `context.include_js/include_css` with BUNDLE names, never raw `/assets/…` paths (one-year cache; already caused a stale-asset bug here) |
| 2026-07-28 | A preview route, `/survey?preview=<version>`, gated by a SEPARATE function rather than a flag | Authors need to see a Draft, which by definition has no campaign and no token. Two functions - `public_survey_payload(token)` with the four anonymous gates, `preview_payload(version)` with logged-in + read permission - because a shared function with an `is_preview` flag is how unpublished content eventually reaches a guest: one wrong caller and the gate inverts. Two functions cannot be called wrongly. | A flag on the existing payload function; a second www page duplicating the template; an anonymous preview token | The preview gate is strictly STRONGER than the anonymous one and relaxes exactly one thing: content need not be Published. It relaxes nothing about writing - preview renders with an empty token and `submit_survey`'s first act is to resolve one. `submit_survey` has no preview parameter and no preview branch, and `check_repo.sh` now fails if it grows one |
| 2026-07-28 | Layout editing moves from Preview into the Questions panel; Preview is pure preview | Direct manipulation belongs where the questions are managed, and Preview's job is to be the real page. The two objections to a grid in the Questions panel both resolved: reorder already means "drop onto card N, take its slot" (not "between"), which is equally well-defined in a grid since the sequence stays linear and CSS Grid only decides where it wraps - so `_wireQuestionDrag` and `_reorder` are unchanged; and the mousedown-vs-`draggable` collision is fixed by turning `draggable` off for exactly as long as the width grip is held. | Insertion indicators computed from pointer position, and moving `draggable` onto the ⋮⋮ handle - both rejected for changing verified reorder behaviour for a cosmetic gain | Drops stay CARDS-only, so row gaps are not drop targets. The 2D rendering is a CSS change, not a handler change: the revert is one line back to `display:flex`. The panel shows PROPORTION, not the respondent's real line lengths - Preview is where accuracy comes from |
| 2026-07-28 | Survey theming is a Single DocType with typed Color fields, not text in Website Theme's custom CSS | Website Theme's custom CSS is a free-text SCSS blob: driving it from a structured editor means parsing and text-substituting inside someone else's stylesheet on every save, where a hand edit that breaks our markers silently corrupts theirs or ours - and saving a Website Theme triggers an SCSS build that can fail. A Single is Frappe's native home for site-wide settings, and its form IS the editor: `Color` renders a picker, `Select` renders the closed font list, permissions, version history and dirty-state come free, and no asset build runs. | Text-munging Website Theme's custom CSS (the existing mechanism - genuinely cannot support it); per-survey theme fields; a custom page with `<input type="color">` | Site-wide, not per-survey: one college, one brand. The picker makes the UI honest; the server-side regex makes it SAFE - a Single can be written through the REST API, `frappe.client.set_value` or the console, none of which involve a picker, so validation is the actual guarantee and not a fallback for a weak widget |
| 2026-07-28 | The theme reaches the guest page as validated variables only, never as stored text | `www/survey.html` is the only guest-reachable page in the app. `theme.build_theme_css()` emits `--<known-name>:#rrggbb;` and a font stack chosen from a hard-coded table - the variable NAMES are a closed list, colours must match `^#[0-9a-f]{6}$`, and the stored font value is a KEY that never reaches the output. So no stored text is echoed: `</style>` cannot be smuggled and no CSS function (`url()`, attribute selectors) can be introduced. Invalid values are DROPPED, not sanitised, so a bad row costs the default colour rather than the page. | A free-form CSS field (refused, standing decision); sanitising instead of dropping | This is the earlier "no staff-editable CSS blob in a guest page" ruling upheld, not weakened - it is the "closed set of validated values" shape that decision already named as acceptable. `test_theme.py` tests it against real payloads; `check_repo.sh` fails if `theme_css` is ever built from anything but `build_theme_css`, if anything else is interpolated into the `<style>` block, or if the hex pattern is loosened |
| 2026-07-28 | Cosmetic customisation is a Select; bespoke styling is a code deploy. No custom-CSS field, constrained or otherwise. | Asked for "change it to whatever we want". The closed-Select pattern extends comfortably to colour and density (~10 more controls) and covers most of that want with zero new risk. A CONSTRAINED custom-CSS mode - allowlisted properties and selectors, regex-validated values, re-emitted from a parse tree - is buildable but was rejected on three grounds: it replaces a provable guarantee ("the output can only ever be `--known-name:#rrggbb;`", 40 readable lines) with a maintained claim ("our allowlist is complete") on the one unauthenticated page in the app; CSS is a hostile parsing target with well-known filter bypasses (escapes like `\75 rl()`, comment-split tokens, `background` shorthand smuggling `url()`); and it would not deliver the actual want anyway, since "whatever we want" means LAYOUT, which no property allowlist provides. | A raw CSS field (refused, standing decision); a constrained/allowlisted CSS parser; an "advanced mode" applied only on the authenticated preview route | Genuinely bespoke styling goes through `public/css/ucc_survey_form.bundle.css` - git, review, `bench build`. That is unlimited customisation with NO runtime injection surface, because the CSS is authored by whoever can already deploy code, which is the honest trust boundary. The preview-only advanced mode was named and rejected specifically: styling you can only see and never ship is worth ~nothing, and its real destiny is a request to promote it to the live page with sunk cost behind it. |
| 2026-07-29 | A survey can be created inline in the Builder (`api/builder.new_survey_with_version`); the raw UCC Survey form is never a required stop | A UCC Survey is a title that owns versions — `naming_series` carries its own default and every other field is optional — so the round trip through a Desk form existed to collect exactly one string. "+ New Version" now takes either an existing survey or a new title, and the title branch creates the survey and its first Draft version in one call, returning the VERSION name so the caller has one code path. | Leaving creation on the Desk form; a "create new" option inside the Link field's dropdown (Frappe's own quick-entry, which reopens the same form) | Both permissions are checked BEFORE anything is written: `create` on UCC Survey and on UCC Survey Version. A user allowed to make versions is not automatically allowed to make surveys, and a half-done create is worse than a refusal. The Desk form still exists and still gets reached from the Awesomebar — `ucc_survey.js` gives it a way back rather than pretending it is gone |
| 2026-07-29 | Publishing does NOT auto-create a campaign. The Builder gets a "Start collecting" action that asks for one field and sets everything else server-side | Upholds the 2026-07-26 ruling (Finding A) rather than reopening it: `Survey Tracking.survey_name` is educ_sg's and stays mandatory, because auto-creating stub planning records would write documents nobody planned into the institutional register this app exists to produce evidence from, and a Property Setter would relax someone else's constraint for everyone. What was actually wrong was the UI: "No campaign points at this version yet" was a true statement with no action attached, and the only way forward was a Survey Tracking form full of fields nobody here owns. So the prerequisite is unchanged and the friction is gone. | Auto-create on publish (needs a stub planning record); relax `survey_name`; a second campaign DocType of our own | The Link's target DocType is read off the live meta (`collection_setup`), never hardcoded — the same rule `api/extract._resolve()` follows. The collection WINDOW is deliberately not set: those fieldnames are still unverified (`survey_tracking_hooks.validate`), and this is a write path, so an unbounded window is honest where a guessed fieldname would silently write nothing. `start_collecting` refuses on a non-Published version and on a version that already has a campaign, and never mints the token itself — `survey_tracking_hooks.validate` does, so one place decides what a public token is |
| 2026-07-29 | `UCC Survey.owner_department` becomes a `Link` to ERPNext's `Department`; unmatchable existing text is preserved in `owner_department_legacy`, never dropped and never left dangling | Felix decided the scope-lock question (BENCH_VERIFY #4) directly: it should be the real Department list, not free text, so a survey can be reported on alongside everything else that department owns. Two facts shaped the migration rather than the decision: `Department` ships with **ERPNext**, not Frappe (verified — `erpnext/setup/doctype/department` exists on version-15, `frappe/core/doctype/department` and hrms's do not), so the app now depends on ERPNext being installed; and a Department's DOCNAME is `f"{department_name} - {company_abbr}"`, so typed text like "Academic Affairs" matches `department_name`, not the docname — a naive `exists("Department", value)` would report every existing value as unmatched. | Leaving it `Data`; matching only on docname; fuzzy/closest-match resolution; blanking unmatched values; leaving unmatched text in the Link column | Unmatched includes AMBIGUOUS: two companies with an "Academic Affairs" give two docnames for one string, and picking either silently reassigns which company's reporting the survey lands in — so it is reported with its candidates, not guessed. Leaving the text in the Link column was rejected because Frappe's `_validate_links()` throws on the NEXT save, firing weeks later on an unrelated edit. The matching rule is a pure module (`department_match.py`) with its own bench-free test, because a patch that rewrites a column is the last place to discover the rule was wrong. `report()` is read-only and runs before migrate; the patch no-ops loudly (Error Log, no writes) if `Department` is not installed |
| 2026-07-29 | Mapping Studio's Canvas becomes the mapping surface: two columns, drag-to-connect writes a real mapping, click an edge to delete. Graph built server-side; `add_`/`remove_question_mapping` are new verbs beside `upsert_`, not a second write path | Canvas was a one-question lineage view behind a toggle that read like a work surface — and its only input was the current selection, which the `ucc-map-q-link` bug had made unreachable. `upsert_question_mapping` could not serve it: it edits ONE row because the inspector it serves has one objective field, so a dropped line would have replaced an objective rather than added one, and thrown outright on a question already carrying two. "Edit the field" and "add an edge" are different verbs on the same document, so they are different methods over one permission gate (`_writable_question`), not one method with a mode flag. | Reusing `upsert` with a flag; building the graph in the browser; a separate canvas DocType; keeping the lineage view alongside | The graph is built by `api.mapping.mapping_canvas` from `map_graph.py` so node ids are issued by the server, not invented by the browser — this is a write surface, and `connect_nodes`/`disconnect_nodes` resolve a dropped pair through the same `connection_pair` rule that produced the ids. Gap flags come from `mapping_coverage`'s list rather than from "has no objectives", so the canvas and the coverage header cannot disagree. Node positions are NOT saved: `UCC Index Node` has `pos_x/pos_y` because a diagram is its content, but adding layout columns to `UCC Survey Question` and `UCC Objective` to remember a drag is not worth it |
| 2026-07-29 | **Gap 2 resolved.** `UCC Objective` is deleted. `UCC Question Mapping.objective` links directly to educ_sg's `Survey Objective` register, which already links each objective to Policies And Standards Management. | The register holds **97** real objectives. `UCC Objective` held **7** — six seeded by `demo_data.py` and one from a trial extraction — because `extraction.objective_code` slugified the source value (`"Graduate Employability"` → `GRADUATE-EMPLOYABILITY`) and made the slug the primary key, on a docstring premise ("educ_sg objective names are free text") that turned out to be false: `Survey Question Item.objective` is a **Link**, so the value was always a docname. The slug therefore destroyed the only join key, and its 60-character truncation would have silently merged two objectives sharing a long prefix. This app had built a weaker parallel model — free-text `primary_clause` on each mapping row — of a relationship the institution already maintains properly, and attached it to the wrong entity. | Keeping `UCC Objective` and syncing it; linking `UCC Question Mapping.standard` to Policies And Standards Management while leaving objectives duplicated (Gap 2 as originally framed); fuzzy-matching slugs back to docnames | Timing made it cheap: 1 real record and 6 demo ones, so the migration is near-empty and will never be cheaper. **Extraction now creates nothing** — a source row pointing at an objective the register does not have is reported in `unknown_objectives` and its mapping skipped, the same refusal as the Survey Management stub (2026-07-26). `demo_data.py` borrows real objectives (deterministic, first six by name) instead of seeding fakes, and creates no mappings at all on a site with an empty register. The migration's rule is a pure module (`objective_migration.py`, 9 tests): relink on `objective_name`, drop `DEMO-` rows, report anything else and KEEP the old DocType rather than orphaning a mapping. `UCC Standard` is untouched and still invented — a smaller version of the same question, left open deliberately |
| 2026-07-29 | **A published question's WORDING may be corrected with an audited reason; its answer-determining structure stays frozen.** New `CORRECTABLE_FIELDS = {"question_text"}` in `versioning.py`, separate from `PRESENTATION_FIELDS`. | Felix needs to fix an obvious typo in a live question without a new version — which would orphan the campaign already collecting. Traced every reader first: **nothing scoring touches `question_text`** (`metric_engine`, `index_engine`, `index_calc` never reference it; `UCC Score Breakdown.lineage_questions` stores docnames), so no number can change. `track_changes` is already 1 on `UCC Survey Question`, so who/when/old/new were already recorded. | Adding `question_text` to `PRESENTATION_FIELDS` (would let wording change silently, or force a reason for a column drag); a "Known issue" note instead of an edit (leaves the typo in front of every future respondent and still forces a new version); requiring a new version (the thing being avoided) | **The invariant moves and this is the honest statement of the cost:** no answer row snapshots the wording a respondent saw, so after a correction the app can no longer prove what was on screen. That is why the exemption was granted ONLY with a marker at the point the evidence is READ — `api/lineage.py` → the Criterion 7.1.1 report, and `api/campaign.py` → the response distribution, both showing "wording corrected" with the reason on hover. An audited correction nobody sees while reading the evidence is a quiet rewrite of the record. Two frozensets, not one, because the permissions differ: presentation is silent, correction demands a reason. A save changing BOTH fails both gates — deliberately, since the recorded reason would describe half of it. `check_repo.sh` fails if the sets overlap or if any answer-determining field (`question_type`, `choices`, `is_required`, `matrix_rows`, `display_logic*`, `survey_version`, `sequence`) is ever exempted |
| 2026-07-31 | **Measurement Outcomes becomes one compact workbench with five PARALLEL workspaces** (Surveys, Objectives, Metrics, Indices, Criterion 7), rendered from the design prototype's own CSS scoped under `.ucc-mo`, mounted via `frappe.ui.make_app_page`. The five existing Desk pages stay reachable and unchanged. | The Frappe-styled pages spent most of the viewport on spacing, repeated headings and summary cards, and results were unreachable from the Builder. The prototype's answer is a fixed-height shell with independently scrolling panes and contextual inspectors. Scoping matters more than it looks: the prototype styles `.btn`, `.field`, `.pane`, `.tab`, `.icon`, `.count`, `.search`, `.status` — every one a name Frappe Desk uses for its own chrome, so loading it unscoped would restyle the whole bench. | A Frappe-form-based redesign (the thing being replaced); rebuilding the backend to match the prototype; deleting the old pages in the same change | The CSS is generated by `scripts/scope_prototype_css.py` FROM the artifact, not transcribed — re-running it against a new prototype is one command rather than a day of transcription that drifts on the first edit. `check_repo.sh` fails on any selector outside `.ucc-mo`, negative-tested. The 5-step progress bar is deliberately NOT reinstated: these are parallel workspaces, and the bar implied every survey walks one linear path. Every state the UI draws is read from the existing backend — `get_survey_builder.editable` decides the lock, `mapping_coverage` decides what is unmapped, `validate_index` decides what is publishable. No rule is re-implemented in JS |
| 2026-07-31 | **OPEN — the metric aggregation rule is not decided, and the Metrics workspace states that rather than assuming one.** | The design brief specifies a "total weight" indicator that goes amber and demands "add N% more" until it reaches 100%. That UI assumes explicit weights. The engine does something else: `aggregate_metric` takes a plain mean over ANSWERS, and `weight_within_metric` is stored and read by nothing — verified by grep across the whole app. Building the amber bar as specified would have demanded a number that changes no score. Worse, the plain mean over answers means a metric spanning a 200-response survey and a 20-response survey is implicitly weighted 10:1 by response volume, which nobody chose. | Building the 100% bar as specified (would demand a meaningless number); silently switching the engine to mean-of-sources (changes existing published numbers with no decision); hiding the weight column (loses information that IS stored) | The workspace shows recorded weights with the caption "Recorded only. The calculation currently averages every answer equally; weighting is an open decision." **Recommendation when Felix decides: mean of SOURCES, with explicit weights as an override** — it matches how people read "this metric is built from 9 questions" and makes cross-survey metrics behave predictably regardless of response volume. It WILL change existing numbers, so it needs its own decision-log entry and a note that results either side are not comparable. Until then no scoring code has been touched |

## 2026-08-01 — Survey grouping for source selection: department, not category

The Metrics source browser groups surveys by `UCC Survey.owner_department`
(a Link to Department) rather than `UCC Survey.category`.

`category` exists but is a **Data / free-text** field, so grouping on it would
fragment the first column on typos and casing — "Student Support",
"student support" and "Stdent Support" would be three groups. `owner_department`
is genuinely controlled and needs no schema change.

It is organisational, not thematic: it answers "who owns this survey", not
"what is this survey for". Felix has no fixed thematic list ready yet, so this
is explicitly an interim choice. Revisit with a controlled `survey_type`
Select (or a `category` converted to Select) once that list exists; the
browser's first column reads from one endpoint (`api.metrics.source_categories`)
precisely so swapping the grouping field is a one-function change.
