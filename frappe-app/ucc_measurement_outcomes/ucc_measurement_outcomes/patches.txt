# Data migration patches. One patch reference per line, e.g.
# ucc_measurement_outcomes.patches.v0_0.some_patch
#
# The section markers matter: patches under [post_model_sync] run AFTER bench
# migrate has synced the DocType JSONs, which is the only order in which a patch
# can write to a field the same release just added.

[pre_model_sync]

[post_model_sync]
ucc_measurement_outcomes.patches.v0_8_0.link_owner_department
