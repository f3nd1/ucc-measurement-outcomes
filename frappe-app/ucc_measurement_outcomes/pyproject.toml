[project]
name = "ucc_measurement_outcomes"
authors = [
    { name = "United Ceres College", email = "felix@unitedceres.edu.sg" },
]
description = "Survey design, mapping, index calculation and Criterion 7.1.1 outcomes for United Ceres College."
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.flit.module]
name = "ucc_measurement_outcomes"
